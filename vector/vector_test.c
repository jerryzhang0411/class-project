/**
 * Machine Problem: Vector
 * CS 241 - Spring 2017
 */

#include "vector.h"
#include <stdio.h>
int main(int argc, char *argv[]) {
/*
  // Write your test cases here
  vector* v = int_vector_create();
  //printf("size: %d", (int)vector_size(v));
  //printf("\n");
  //printf("size: %d", (int)vector_capacity(v));
  //printf("\n");
  if(vector_empty(v)) printf("true\n");
  //int a = 0;
  int* one = malloc(sizeof(int));
  *one = 1;
  //int b = 1;
  int* two = malloc(sizeof(int));
  *two = 2;
  int* three = malloc(sizeof(int));
  *three = 3;
  int* four = malloc(sizeof(int));
  *four = 4;
  int* five = malloc(sizeof(int));
  *five = 5;
  int* six = malloc(sizeof(int));
  *six = 6;

  vector_push_back(v, one);
  vector_push_back(v, two);
  vector_push_back(v, three);
  vector_push_back(v, four);
  vector_push_back(v, five);
  vector_push_back(v, six);

  //printf("testing resize\n");
  //printf("capacity before is : %zu\n", vector_capacity(v));
  vector_resize(v,3);
  //printf("size now becomes %zu\n", vector_size(v));
  //printf("capacity after is : %zu\n", vector_capacity(v));
  vector_resize(v,10);

  //for(int i = 0;i < (int)vector_size(v);i++){
  	//int temp = (int)vector_at(v,i);
	//printf("The %dth loop is: %d\n", i, temp);
  //}


  //printf("size: %d\n", (int)vector_size(v));
  //printf("size: %d\n", (int)vector_capacity(v));

  //printf("getting element @ 3\n");
  //printf("element @ 3 is: %d\n", *(int*)vector_get(v, 3));

  //printf("setting element @ 3\n");
  int x = 5;
  int *set = &x;
  vector_set(v, 3, set);
  //printf("element @ 3 after resetting is: %d\n", *(int*)vector_get(v, 3));

  //printf("testing resize\n");
  vector_push_back(v, five);
  vector_push_back(v, five);
  vector_push_back(v, five);
  vector_push_back(v, five); // 10th
  //printf("size: %d\n", (int)vector_size(v));
  //printf("capacity: %d\n", (int)vector_capacity(v));
  vector_push_back(v, five); // 11th
  //printf("size: %d\n", (int)vector_size(v));
  //printf("capacity: %d\n", (int)vector_capacity(v));

  //printf("testing front\n");
  //printf("element @ front is: %d\n", *(int*)vector_front(v));

  
  //printf("testing back\n");
  //printf("element @ back is: %d\n", *(int*)vector_back(v));
  
  
  //printf("testing insert @ 3,4\n");
  x = 9;
  int *insert = &x;
  vector_insert(v, 3, insert);
  //printf("3 after insert is: %d\n", *(int*)vector_get(v,3));
  //printf("4 after insert is: %d\n", *(int*)vector_get(v,4));

  //printf("testing clear\n");
  vector_clear(v);
  
  //printf("testing destroy\n");
  
  vector_destroy(v);
  v = NULL;
  free(one);
  one = NULL;
  free(two);
  two = NULL;
  free(three);
  three = NULL;
  free(four);
  four = NULL;
  free(five);
  five = NULL;
  free(six);
  six = NULL;
*/
  
  return 1;
  
}

