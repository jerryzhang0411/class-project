/**
 * Machine Problem: Vector
 * CS 241 - Spring 2017
 */

#include <assert.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "document.h"
#include "vector.h"
#include <stdio.h>

 struct document {
  vector *vector;
};

int main(int argc, char *argv[]) {
/*
	assert(argc == 2);
	const char * path_to_file= argv[1];
  	document* ret = document_create_from_file(path_to_file);
  	vector * v= ret->vector;

	for(int i = 0; i<(int)vector_size(v);i++){
   		char* x= (char*)*(vector_at(v, i));
  		printf("%s\n", x);
	}


//
 * Inserts a copy of 'str' into the 'line_number'-th line of 'document'.
 *
 * Note: Inserting into the middle of the document shifts all subsequent lines
 * down and inserting past the end fills the gap with empty strings.
 *
 * Note: lines in document are 1-indexed.
 *
 * Note: Inserting NULL is invalid. The document can only contain non-NULL
 * entries.
 //

document_insert_line(ret, 2, "const char *str");


document_write_to_file(ret, "wala_output.txt");

document_destroy(ret);

ret = document_create_from_file("wala_output.txt");
 v= ret->vector;

for(int i = 0; i<(int)vector_size(v);i++){
   char* x= (char*)*(vector_at(v, i));
  printf("%s\n", x);
}


document_destroy(ret);
*/
  return 0;

}